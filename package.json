{
	"name": "yandex-chat",
	"version": "0.0.0",
	"description": "Independent project",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "npm run clear && parcel build ./src/pages/index.hbs --no-cache",
		"start": "npm run build && node server.js --open",
		"dev": "npm run clear && parcel -p 3000 ./src/pages/index.hbs --no-cache --open",
		"clear": "rm -rf dist"
	},
	"author": "belk1ng",
	"license": "MIT",
	"dependencies": {
		"express": "^4.18.2",
		"handlebars": "^4.7.7",
		"node-sass": "^8.0.0"
	},
	"devDependencies": {
		"@anikin/parcel-transformer-handlebars": "^1.0.9",
		"@parcel/transformer-sass": "^2.8.3",
		"parcel": "^2.8.3"
	}
}
